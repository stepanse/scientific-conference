apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scientific-congress-backend-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: "stepanse-scientific-conference-backend.tutik"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: scientific-conference-backend-svc
            port:
              number: 80
